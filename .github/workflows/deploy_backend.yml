name: Placeholder Backend Deploy Workflow

on:
  workflow_dispatch:

jobs:
  noop:
    name: No-Op Backend Job
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder Step
        run: echo "Backend deploy workflow placeholder - not active yet"
