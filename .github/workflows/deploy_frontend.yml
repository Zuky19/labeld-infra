name: Placeholder Frontend Deploy Workflow 

on:
  workflow_dispatch: 

jobs:
    noop:
      name: No-Op Frontend Job 
      runs-on: ubuntu-latest 
      steps: 
         - name: Placeholder Step 
           run: echo "Frontend deploy workflow placeholder - not active yet"
      
